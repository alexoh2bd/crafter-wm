Development of code under this directory has used **[Cursor](https://cursor.com)** (AI-assisted editing and agents).

---

`src/` is the Python package root: **LeWM** (`lemodel.py`, `letrain.py`, …), **HWM** (`hwm/`), launch **scripts** (`scripts/`), and related utilities. Set `PYTHONPATH=src` when running modules from the repo root.
